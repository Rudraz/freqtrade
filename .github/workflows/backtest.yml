name: Backtest

on:
  push:
    branches: [ main ]
    paths:
      - 'user_data/**'
      - 'docker-compose.yml'
      - '.github/workflows/backtest.yml'
  pull_request:
    paths:
      - 'user_data/**'
      - 'docker-compose.yml'
      - '.github/workflows/backtest.yml'
  workflow_dispatch: {}